type: object
properties:
  sensorId:
    type: string
    description: The ID of the sensor
  sensorType:
    $ref: 'typeDto.yaml'
  sensorNick:
    type: string
    nullable: true
    description: The nickname of the sensor
  categories:
    type: array
    nullable: true
    description: The categories of the sensor
    items:
      $ref: 'categoryDto.yaml'